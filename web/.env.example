# Base URL of the Spring Boot backend. Leave unset for local dev — the Vite
# dev server proxies /api to http://localhost:8080 (see vite.config.ts).
# In production (Vercel project settings) point it at the deployed backend:
# VITE_API_BASE_URL=https://upi-offline-mesh.onrender.com
